function editProfile() { var user = Storage.get('sticky_user', {}), name = window.prompt('氏名を入力してください', user.displayName || ''), org; if (!name) { return; } org = window.prompt('部署名を入力してください（半角英数字のみ）', user.organization || ''); if (!org || !/^[A-Za-z0-9]+$/.test(org)) { Fsn.toast('部署名は半角アルファベットと数字のみです。'); return; } user.displayName = name.replace(/^\s+|\s+$/g, ''); user.organization = org; Storage.set('sticky_user', user); document.getElementById('user-name').textContent = user.displayName; document.getElementById('org-name').textContent = user.organization; Audit.log('UPDATE_PROFILE', user.displayName + ' / ' + user.organization); if (APP_CONFIG.USE_SHAREPOINT) { StickyUsersApi.ensureUser(user, function () { StickyGroupsApi.ensureMembership(user.organization, user.userId, function () {}); }); } Fsn.toast('プロフィールを更新しました。'); }
document.addEventListener('DOMContentLoaded', function () { var nav = document.querySelector('.topbar nav'), button; if (!nav) { return; } button = document.createElement('button'); button.className = 'nav-tab'; button.type = 'button'; button.textContent = 'プロフィール'; button.onclick = editProfile; nav.appendChild(button); });
