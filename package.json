{
  "name": "fsn",
  "version": "0.2.0",
  "private": true,
  "description": "Offline ES5 SharePoint sticky app; npm is only needed for development tests",
  "scripts": { "test": "node --test tests/*.test.cjs", "start": "node tools/serve.cjs" },
  "devDependencies": { "acorn": "8.15.0", "jsdom": "26.1.0" }
}
